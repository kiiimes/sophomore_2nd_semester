Rectangle 클래스 만들기
드라이버 클래스가 꼭 필요함 클래스 만들고 나서 
실행할 수 있는 클래스가 필요한거

MyPoint 
data : x,y
method : 좌표가 어느 사분면에 있는지 체크하는 메소드
1,2,3,4 값을 리턴

static을 데이터앞에 붙일수도 있고 메소드 앞에 붙일수도 있따. 
둘다 클래스의 이름으로 접근할 수 있는데 
데이터가 private으로 선언되면 클래스의이름으로도 접근x

데이터에static붙이면 공유하는데이터가된다. 
static 메소드 안에서는 static 멤버만 접근이 가능하다. 
static메소드 안에서는this를 쓸 수 없다. 

하나의 메소드에서 static 데이터의 내용을 바꾸면 다른 함수에서도 
바뀜

클래스에 final 붙이면 상속x 
보통 상수 선언할때 final static FT 이런식으로도 많이 한다. 
final로 메소드 정의하면 오버로딩은 되지만 오버라이딩은x
오버라이딩 => 재정의

label imageicon 자바 어플리케이션으로 테스트 가능해야함
과제란에 .java파일 압축해서 올리셈 
폴더에 src에 있는 자바 파일들을 압축해서 올리면 됨 

데이터랑 메소드 어디에 나올지 모르지만 final static 나옴
이미지 아이콘 나옴
도형 그리는거 안나옴
레이아웃 겹치는거 나옴 nestedpanel
try catch final
메소드 안에 제어문 쓰는거 나옴
배열x
random 쓰는거 있음 