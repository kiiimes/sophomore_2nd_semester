#include <stdio.h>

struct complex {
	double real, imag; };
void add_compl(struct complex st,struct complex str1){



}





void main( ) {  
	int i;
	struct complex x[2] = {0};


	scanf("%d %d",&x.imag,&x.real);

	add_compl(x.imag,x.real);



for( i=0; i < 3 ; ++i )
	printf("x[%d]: %.1f + %.1fi\n", i, x[i].real, x[i].imag);
}