#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main(){
	int n,*p,d,i;
	scanf("%d",&n);
	p=(int*)malloc(n*sizeof(int));
	for(i=0;i<n;i++){
		scanf("%d",p+i);
	}
	scanf("%d",&d);

	for(



}